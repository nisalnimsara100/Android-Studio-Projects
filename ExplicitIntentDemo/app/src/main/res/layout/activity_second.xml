<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout for the SecondActivity.
    Arranges UI elements vertically.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutSecond"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SecondActivity"
    android:orientation="vertical"
    android:padding="24dp" >

    <!-- Title TextView: Displays a welcome message -->
    <TextView
        android:id="@+id/textViewWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome!"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="16dp"/>

    <!-- TextView: Displays the name received from MainActivity -->
    <TextView
        android:id="@+id/textViewResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Name will appear here"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal"/>

    <!-- Button: User can confirm after viewing the name -->
    <Button
        android:id="@+id/buttonThanks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm"
        android:layout_marginTop="16dp"
        android:padding="12dp"/>

</LinearLayout>
